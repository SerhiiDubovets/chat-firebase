import styled from "styled-components";

import { flexSpaceBetween } from "@/styles/mixins";

export const SendMessageFormStyle = styled.form`
  ${flexSpaceBetween}
  padding-block: 0.75rem;
  padding-inline: 1.5rem;
  gap: 10px;
`;
