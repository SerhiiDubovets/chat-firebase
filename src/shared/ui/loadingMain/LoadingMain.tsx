import { LoadingBlockStyle, LoadingStyle } from "./loadingMain.style";

const LoadingMain = () => {
  return (
    <LoadingBlockStyle>
      <LoadingStyle>Loading...</LoadingStyle>
    </LoadingBlockStyle>
  );
};

export default LoadingMain;
